services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: records
      POSTGRES_PASSWORD: records
      POSTGRES_DB: recordsbl
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
